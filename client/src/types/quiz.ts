export interface Question {
    id: number;
    question : string;
    options : string[];
    correctAnswer : number;
    // sectionId: number;
    // title: string;
    // description: string;
    // duration: number;
    // passingScore: number;
    // questions: object;
    // createdAt: Date;
    // updatedAt: Date;
    // email_verified: boolean;
   // section: Section;
}
